{
  "name": "lttimer",
  "version": "1.0.0",
  "description": "timer for lt",
  "main": "index.js",
  "scripts": {
    "watch": "babel --watch src -d public",
    "build": "babel src -d public",
    "lint": "eslint src",
    "fix": "eslint --fix src",
    "serve": "npm run build && browser-sync start --server 'public' --files 'public'",
    "deploy": "npm run build && gh-pages --dist public"
  },
  "author": "hogashi",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "browser-sync": "^2.24.4",
    "eslint": "^4.19.1",
    "gh-pages": "^1.1.0"
  }
}
